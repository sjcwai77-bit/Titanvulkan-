--- a/app/src/main/cpp/vulkan_renderer.cpp
+++ b/app/src/main/cpp/vulkan_renderer.cpp
@@ -182,7 +182,12 @@
     uint32_t count=caps.minImageCount+1;if(caps.maxImageCount&&count>caps.maxImageCount)count=caps.maxImageCount;
     VkSwapchainCreateInfoKHR ci{VK_STRUCTURE_TYPE_SWAPCHAIN_CREATE_INFO_KHR};ci.surface=surface_;ci.minImageCount=count;ci.imageFormat=chosen.format;ci.imageColorSpace=chosen.colorSpace;ci.imageExtent=extent_;ci.imageArrayLayers=1;ci.imageUsage=VK_IMAGE_USAGE_COLOR_ATTACHMENT_BIT;
     uint32_t qfs[]={graphicsFamily_,presentFamily_};if(graphicsFamily_!=presentFamily_){ci.imageSharingMode=VK_SHARING_MODE_CONCURRENT;ci.queueFamilyIndexCount=2;ci.pQueueFamilyIndices=qfs;}else ci.imageSharingMode=VK_SHARING_MODE_EXCLUSIVE;
-    ci.preTransform=caps.currentTransform;ci.compositeAlpha=VK_COMPOSITE_ALPHA_OPAQUE_BIT_KHR;ci.presentMode=VK_PRESENT_MODE_FIFO_KHR;ci.clipped=VK_TRUE;
+    ci.preTransform=caps.currentTransform;
+    VkCompositeAlphaFlagBitsKHR alpha=VK_COMPOSITE_ALPHA_OPAQUE_BIT_KHR;
+    for(auto a:{VK_COMPOSITE_ALPHA_OPAQUE_BIT_KHR,VK_COMPOSITE_ALPHA_INHERIT_BIT_KHR,VK_COMPOSITE_ALPHA_PRE_MULTIPLIED_BIT_KHR,VK_COMPOSITE_ALPHA_POST_MULTIPLIED_BIT_KHR}){if(caps.supportedCompositeAlpha&a){alpha=a;break;}}
+    uint32_t pmc=0;vkGetPhysicalDeviceSurfacePresentModesKHR(physical_,surface_,&pmc,nullptr);std::vector<VkPresentModeKHR> pms(pmc);vkGetPhysicalDeviceSurfacePresentModesKHR(physical_,surface_,&pmc,pms.data());
+    VkPresentModeKHR pm=VK_PRESENT_MODE_FIFO_KHR;for(auto m:pms)if(m==VK_PRESENT_MODE_MAILBOX_KHR){pm=m;break;}
+    ci.compositeAlpha=alpha;ci.presentMode=pm;ci.clipped=VK_TRUE;
     if(!vkok(vkCreateSwapchainKHR(device_,&ci,nullptr,&swapchain_),"vkCreateSwapchainKHR"))return false;
     vkGetSwapchainImagesKHR(device_,swapchain_,&count,nullptr);swapImages_.resize(count);vkGetSwapchainImagesKHR(device_,swapchain_,&count,swapImages_.data());swapViews_.resize(count);
     for(uint32_t i=0;i<count;i++){VkImageViewCreateInfo vi{VK_STRUCTURE_TYPE_IMAGE_VIEW_CREATE_INFO};vi.image=swapImages_[i];vi.viewType=VK_IMAGE_VIEW_TYPE_2D;vi.format=swapFormat_;vi.subresourceRange.aspectMask=VK_IMAGE_ASPECT_COLOR_BIT;vi.subresourceRange.levelCount=1;vi.subresourceRange.layerCount=1;if(!vkok(vkCreateImageView(device_,&vi,nullptr,&swapViews_[i]),"swap image view"))return false;}
@@ -257,10 +262,10 @@
     if(acq==VK_ERROR_OUT_OF_DATE_KHR)return recreateSwapchain();if(acq!=VK_SUCCESS&&acq!=VK_SUBOPTIMAL_KHR)return false;
     vkResetFences(device_,1,&inFlight_[frame_]);
 
-    GlobalUBO u{};u.viewProj=viewProj;u.cameraPosTime[0]=cameraPos.x;u.cameraPosTime[1]=cameraPos.y;u.cameraPosTime[2]=cameraPos.z;u.cameraPosTime[3]=timeSeconds;u.lightDirExposure[0]=-.42f;u.lightDirExposure[1]=-.82f;u.lightDirExposure[2]=.38f;u.lightDirExposure[3]=1.15f;std::memcpy(uniformMapped_[frame_],&u,sizeof(u));
+    GlobalUBO u{};u.viewProj=viewProj;u.cameraPosTime[0]=cameraPos.x;u.cameraPosTime[1]=cameraPos.y;u.cameraPosTime[2]=cameraPos.z;u.cameraPosTime[3]=timeSeconds;u.lightDirExposure[0]=-.36f;u.lightDirExposure[1]=-.88f;u.lightDirExposure[2]=.31f;u.lightDirExposure[3]=1.32f;std::memcpy(uniformMapped_[frame_],&u,sizeof(u));
 
     VkCommandBuffer cmd=commandBuffers_[frame_];vkResetCommandBuffer(cmd,0);VkCommandBufferBeginInfo bi{VK_STRUCTURE_TYPE_COMMAND_BUFFER_BEGIN_INFO};vkBeginCommandBuffer(cmd,&bi);
-    VkClearValue clear[2]{};clear[0].color={{.012f,.035f,.065f,1.0f}};clear[1].depthStencil={1.0f,0};VkRenderPassBeginInfo rp{VK_STRUCTURE_TYPE_RENDER_PASS_BEGIN_INFO};rp.renderPass=renderPass_;rp.framebuffer=framebuffers_[imageIndex];rp.renderArea.extent=extent_;rp.clearValueCount=2;rp.pClearValues=clear;vkCmdBeginRenderPass(cmd,&rp,VK_SUBPASS_CONTENTS_INLINE);
+    VkClearValue clear[2]{};clear[0].color={{.0025f,.008f,.020f,1.0f}};clear[1].depthStencil={1.0f,0};VkRenderPassBeginInfo rp{VK_STRUCTURE_TYPE_RENDER_PASS_BEGIN_INFO};rp.renderPass=renderPass_;rp.framebuffer=framebuffers_[imageIndex];rp.renderArea.extent=extent_;rp.clearValueCount=2;rp.pClearValues=clear;vkCmdBeginRenderPass(cmd,&rp,VK_SUBPASS_CONTENTS_INLINE);
     VkViewport viewport{0,0,float(extent_.width),float(extent_.height),0,1};VkRect2D scissor{{0,0},extent_};vkCmdSetViewport(cmd,0,1,&viewport);vkCmdSetScissor(cmd,0,1,&scissor);vkCmdBindPipeline(cmd,VK_PIPELINE_BIND_POINT_GRAPHICS,pipeline_);vkCmdBindDescriptorSets(cmd,VK_PIPELINE_BIND_POINT_GRAPHICS,pipelineLayout_,0,1,&descriptorSets_[frame_],0,nullptr);
     for(size_t i=0;i<meshes.size();i++){
         const auto&m=meshes[i];const auto&g=gpuMeshes_[i];if(!m.visible||!g.indexCount||!g.vertex.handle)continue;
